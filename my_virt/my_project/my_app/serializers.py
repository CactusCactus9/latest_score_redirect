# A serializer converts Python objects (like your Score model) into JSON, which can be sent to the frontend.

# from rest_framewrk import serializers
# from my_app.models import Score

# class ScoreSerializer(serializers.ModelSerializer):
#     class Meta:
#         model = Score
#         fields= ['player_name', 'score', 'date']  #This tells DRF how to convert Score objects into JSON.

